import { Component } from '@angular/core';

@Component({
  standalone: true,
  selector: 'hog-inventory',
  template: `
    <h1 class="text-2xl font-semibold">Inventory</h1>
    <p class="text-sm text-neutral-500">Org-level inventory management hub (wire later)</p>
  `
})
export class InventoryPage {}
